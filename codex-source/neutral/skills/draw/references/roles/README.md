# Role index

The root coordinator absorbs orchestration and is not a child role. It adopts
these nine references sequentially by default: specification, design,
architecture, task planning, implementation, validation, code review,
security, and deployment readiness.

Optional custom agents consume the same inputs and outputs. They change
execution strategy, not artifact semantics or state ownership.
