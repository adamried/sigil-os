# API development overlay

Add checks for contract compatibility, input/output schemas, status semantics,
authorization at boundaries, versioning, pagination, and error behavior. Do
not weaken the implementation role’s safety or scope constraints.
