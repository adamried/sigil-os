# Integration development overlay

Add checks for authentication delegation, timeouts, retries, idempotency,
rate limits, partial failure, circuit breaking, external-write authorization,
and secret-free diagnostics.
