# Application-security overlay

Add checks for injection, broken access control, authentication/session flaws,
unsafe deserialization, dependency exposure, sensitive logging, and insecure
defaults. High and critical findings activate the security gate.
