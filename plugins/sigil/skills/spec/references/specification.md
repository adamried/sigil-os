# Specification workflow

Use the shared specification role and procedure under
`../../draw/references/roles/specification.md` and
`../../draw/references/procedures/specification.md`.

Standalone spec work does not change workflow state. When draw invokes it, the
root coordinator validates `spec.md` or `clarifications.md` before committing
the relevant transition.
