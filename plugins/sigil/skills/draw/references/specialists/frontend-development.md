# Frontend development overlay

Add checks for component boundaries, interaction states, keyboard use, screen
reader semantics, responsive behavior, existing design patterns, and
client/server separation.
