# Sigil OS — Audit Log

> Append-only log of workflow events. Enable/disable via `/sigil-config set audit_mode true|false`.
> View with `/sigil-audit`. Archive with `/sigil-audit clear`.

<!-- AUDIT-LOG-START — Do not remove this marker. Entries are appended below. -->
